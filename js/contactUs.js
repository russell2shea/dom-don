// Add an event listener to the submit button
document.getElementById("contact_form-submit").addEventListener("click", validateForm);

// Check if the form is valid
function validateForm(){
    console.log('hey');
}